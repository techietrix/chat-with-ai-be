{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: dev",
            "request": "launch",
            "command": "node server.js",
            "cwd": "${workspaceFolder}"
        }
    ]
}